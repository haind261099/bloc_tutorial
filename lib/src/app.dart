import 'package:flutter/material.dart';
import 'package:login_form/src/resource/login_page.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
      home: LoginPage()
    );
  }
}